bin/
obj/
*.user
artifacts/
TestResults/

# OAuth 클라이언트 설정은 절대 커밋하지 않는다.
client_secret*.json
settings.json

# 빌드 시 바이너리에 임베드되는 OAuth 클라이언트. 공개 저장소에 올리면
# GitHub 비밀 스캐닝이 Google에 통보하고 시크릿이 무효화된다.
secrets/
